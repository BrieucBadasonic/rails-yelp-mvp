<h1>Create a new restaurant</h1>


<%= simple_form_for @restaurant do |f| %>
  <%= f.input :name %>
  <%= f.input :address %>
  <%= f.input :phone_number, as: :tel %>
  <%= f.input :category, collection: Restaurant::CAT  %>
  <%= f.submit "Create", class: 'btn btn-large btn-danger' %>
<% end %>
